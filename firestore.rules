rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow anyone to read people, allow logged in users to create/update
    match /people/{personId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
